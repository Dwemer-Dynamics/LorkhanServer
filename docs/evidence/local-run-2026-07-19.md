# Local validation run — 2026-07-19

Environment observed locally: macOS Darwin 25.5.0; PHP 8.5.8; Composer 2.10.2; PostgreSQL tools 16.14. The repositories were dirty, so these are current-worktree observations, not clean-commit durable proof. This is not WSL, Windows, Apache, systemd, browser, CI, OpenMW, or in-game evidence.

| Command | Result |
| --- | --- |
| `php tests/run.php` | PASS — server foundation/media checks |
| `scripts/test/management-http-integration.sh` | PASS — dependency-free browser-like real HTTP login, cookies, hidden CSRF, all navigation/forms, accessible labels/errors, valid/invalid persistence and logout |
| `scripts/test/integration.sh` | PASS — disposable PostgreSQL direct Router/media/provider slice plus ordered migration and durable-job tests |
| `ALMSIVI_CLIENT_ROOT=/Users/reece_latitude/Projects/ALMSIVI/.claude/worktrees/independent-client-foundations ALMSIVI_BEAST_PG_PORT=55475 ALMSIVI_BEAST_HTTP_PORT=58475 scripts/test/beast-http-integration.sh` | PASS — real multi-worker PHP HTTP plus turn worker, disposable PostgreSQL, immediate acceptance, Beast polling/dynamic `ai.follow`/action-result replay/interruption/provider timeout, long-poll cancellation and mid-flow HTTP restart |
| `ALMSIVI_CLIENT_ROOT=/Users/reece_latitude/Projects/ALMSIVI/.claude/worktrees/independent-client-foundations scripts/verify-protocol-parity.sh` | PASS — active implementation worktree protocol is byte-identical; full cross-repo `--require-jsonschema` is BLOCKED because no approved interpreter with `jsonschema` is available and installation is forbidden |
| Root-only `/Users/reece_latitude/Projects/ALMSIVI` cross-repo/Beast lanes | BLOCKED — root checkout lacks the protocol tree/CMake target; nested active implementation worktree was not relabelled as root-only proof |
| Active client `scripts/protocol/validate.py --require-jsonschema` | BLOCKED locally — approved Python lacks `jsonschema`; no dependency was installed |
| `php scripts/protocol/verify-local.php` | PASS — 50 files match manifest/checksums (19 schemas, 31 fixtures) |
| `scripts/verify-protocol-parity.sh /Users/reece_latitude/Projects/ALMSIVI/.claude/worktrees/independent-client-foundations` | PASS — active ALMSIVI worktree schemas, fixtures, manifest and checksums byte-identical |
| PHP lint over `src`, `public`, `config`, `tests`, `scripts`, `workers` | PASS |
| Disposable migration fresh/up/idempotent/rerun/down/rebuild and checksum-drift rejection | PASS locally; not WSL evidence |
| Disposable `pg_dump -Fc` / `pg_restore --exit-on-error` into a fresh database with schema invariant check | PASS locally; not a production backup policy/WSL drill |
| Product-foundation migration/repository/management tests | PASS — revision/rollback, mock-provider secret boundary, memory retrieval/rebuild, knowledge traces, relationship audit, narrative export, browser session/CSRF and pairing rotation/revocation |

The real HTTP lane restarts the PHP listener mid-flow while retaining PostgreSQL, verifies a changed listener PID, reconnects, and replays durable terminal state. Turns now persist acceptance plus a `turn.process` job before the immediate 202; bounded workers own provider/TTS completion and use provider-neutral cooperative cancellation from durable turn/session state, lease loss and deadlines. The expanded integration covers delivery replay/mismatch, health, auth/query credentials, malformed JSON, rate limits, mutation idempotency, generations, negotiated capabilities, turns, bounded polling, provider attempt reconciliation/redaction, deterministic TTS, authenticated opaque media, terminal interruption/action rules, session ending, migrations, leases, restart recovery, retry/dead-letter/replay, and browser-like management forms. Apache/systemd templates were not exercised.
